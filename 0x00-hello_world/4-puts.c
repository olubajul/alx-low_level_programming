#include <stdio.h>
/**
 * main -  prints with puts function, followed by a new line
 * Return: Always 0.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
