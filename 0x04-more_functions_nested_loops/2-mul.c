#include "main.h"

/**
 * mul - multiplies two integers
 * @x: the first number to multiply
 * @y: the second number to multiply with first number
 * Return: the multiiplication result between x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
